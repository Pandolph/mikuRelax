<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="UserManager" parent="AppTheme">
    </style>

    <style name="UserManager.Head" parent="UserManager">
        <item name="android:background">@color/user_manager_status_bar</item>
        <item name="android:height">@dimen/user_manager_status_bar_height</item>
    </style>

</resources>
